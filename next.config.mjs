/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'source.unsplash.com', 'plus.unsplash.com'], // Add the domains you want to use for external images
    },
  };
  
  export default nextConfig;